/*
 * Copyright 2015-2018 The MathWorks, Inc.
 */

try {
    project.catkin.tree.generateMessageArtifact(project, 'radar_msgs')
} catch (NullPointerException e) {
    println("Couldn't find radar_msgs on the ROS_PACKAGE_PATH")
}

task bugfixtask << {
    mkdir sourceSets.main.output.classesDir
}
jar.dependsOn(bugfixtask)
