classdef RadarProcessedArray < ros.Message
    %RadarProcessedArray MATLAB implementation of radar_msgs/RadarProcessedArray
    %   This class was automatically generated by
    %   ros.msg.internal.gen.MessageClassGenerator.
    
    %   Copyright 2014-2020 The MathWorks, Inc.
    
    %#ok<*INUSD>
    
    properties (Constant)
        MessageType = 'radar_msgs/RadarProcessedArray' % The ROS message type
    end
    
    properties (Constant, Hidden)
        MD5Checksum = 'cf9fa8e0a4edda9688b9f7533e8ed498' % The MD5 Checksum of the message definition
    end
    
    properties (Access = protected)
        JavaMessage % The Java message object
    end
    
    properties (Constant, Access = protected)
        RadarMsgsRadarProcessedClass = ros.msg.internal.MessageFactory.getClassForType('radar_msgs/RadarProcessed') % Dispatch to MATLAB class for message type radar_msgs/RadarProcessed
        StdMsgsHeaderClass = ros.msg.internal.MessageFactory.getClassForType('std_msgs/Header') % Dispatch to MATLAB class for message type std_msgs/Header
    end
    
    properties (Dependent)
        Header
        Groundtruth
    end
    
    properties (Access = protected)
        Cache = struct('Header', [], 'Groundtruth', []) % The cache for fast data access
    end
    
    properties (Constant, Hidden)
        PropertyList = {'Groundtruth', 'Header'} % List of non-constant message properties
        ROSPropertyList = {'groundtruth', 'header'} % List of non-constant ROS message properties
    end
    
    methods
        function obj = RadarProcessedArray(msg)
            %RadarProcessedArray Construct the message object RadarProcessedArray
            import com.mathworks.toolbox.ros.message.MessageInfo;
            
            % Support default constructor
            if nargin == 0
                obj.JavaMessage = obj.createNewJavaMessage;
                return;
            end
            
            % Construct appropriate empty array
            if isempty(msg)
                obj = obj.empty(0,1);
                return;
            end
            
            % Make scalar construction fast
            if isscalar(msg)
                % Check for correct input class
                if ~MessageInfo.compareTypes(msg(1), obj.MessageType)
                    error(message('ros:mlros:message:NoTypeMatch', obj.MessageType, ...
                        char(MessageInfo.getType(msg(1))) ));
                end
                obj.JavaMessage = msg(1);
                return;
            end
            
            % Check that this is a vector of scalar messages. Since this
            % is an object array, use arrayfun to verify.
            if ~all(arrayfun(@isscalar, msg))
                error(message('ros:mlros:message:MessageArraySizeError'));
            end
            
            % Check that all messages in the array have the correct type
            if ~all(arrayfun(@(x) MessageInfo.compareTypes(x, obj.MessageType), msg))
                error(message('ros:mlros:message:NoTypeMatchArray', obj.MessageType));
            end
            
            % Construct array of objects if necessary
            objType = class(obj);
            for i = 1:length(msg)
                obj(i,1) = feval(objType, msg(i)); %#ok<AGROW>
            end
        end
        
        function header = get.Header(obj)
            %get.Header Get the value for property Header
            if isempty(obj.Cache.Header)
                obj.Cache.Header = feval(obj.StdMsgsHeaderClass, obj.JavaMessage.getHeader);
            end
            header = obj.Cache.Header;
        end
        
        function set.Header(obj, header)
            %set.Header Set the value for property Header
            validateattributes(header, {obj.StdMsgsHeaderClass}, {'nonempty', 'scalar'}, 'RadarProcessedArray', 'Header');
            
            obj.JavaMessage.setHeader(header.getJavaObject);
            
            % Update cache if necessary
            if ~isempty(obj.Cache.Header)
                obj.Cache.Header.setJavaObject(header.getJavaObject);
            end
        end
        
        function groundtruth = get.Groundtruth(obj)
            %get.Groundtruth Get the value for property Groundtruth
            if isempty(obj.Cache.Groundtruth)
                javaArray = obj.JavaMessage.getGroundtruth;
                array = obj.readJavaArray(javaArray, obj.RadarMsgsRadarProcessedClass);
                obj.Cache.Groundtruth = feval(obj.RadarMsgsRadarProcessedClass, array);
            end
            groundtruth = obj.Cache.Groundtruth;
        end
        
        function set.Groundtruth(obj, groundtruth)
            %set.Groundtruth Set the value for property Groundtruth
            if ~isvector(groundtruth) && isempty(groundtruth)
                % Allow empty [] input
                groundtruth = feval([obj.RadarMsgsRadarProcessedClass '.empty'], 0, 1);
            end
            
            validateattributes(groundtruth, {obj.RadarMsgsRadarProcessedClass}, {'vector'}, 'RadarProcessedArray', 'Groundtruth');
            
            javaArray = obj.JavaMessage.getGroundtruth;
            array = obj.writeJavaArray(groundtruth, javaArray, obj.RadarMsgsRadarProcessedClass);
            obj.JavaMessage.setGroundtruth(array);
            
            % Update cache if necessary
            if ~isempty(obj.Cache.Groundtruth)
                obj.Cache.Groundtruth = [];
                obj.Cache.Groundtruth = obj.Groundtruth;
            end
        end
    end
    
    methods (Access = protected)
        function resetCache(obj)
            %resetCache Resets any cached properties
            obj.Cache.Header = [];
            obj.Cache.Groundtruth = [];
        end
        
        function cpObj = copyElement(obj)
            %copyElement Implements deep copy behavior for message
            
            % Call default copy method for shallow copy
            cpObj = copyElement@ros.Message(obj);
            
            % Clear any existing cached properties
            cpObj.resetCache;
            
            % Create a new Java message object
            cpObj.JavaMessage = obj.createNewJavaMessage;
            
            % Recursively copy compound properties
            cpObj.Header = copy(obj.Header);
            cpObj.Groundtruth = copy(obj.Groundtruth);
        end
        
        function reload(obj, strObj)
            %reload Called by loadobj to assign properties
            obj.Header = feval([obj.StdMsgsHeaderClass '.loadobj'], strObj.Header);
            GroundtruthCell = arrayfun(@(x) feval([obj.RadarMsgsRadarProcessedClass '.loadobj'], x), strObj.Groundtruth, 'UniformOutput', false);
            obj.Groundtruth = vertcat(GroundtruthCell{:});
        end
    end
    
    methods (Access = ?ros.Message)
        function strObj = saveobj(obj)
            %saveobj Implements saving of message to MAT file
            
            % Return an empty element if object array is empty
            if isempty(obj)
                strObj = struct.empty;
                return
            end
            
            strObj.Header = saveobj(obj.Header);
            strObj.Groundtruth = arrayfun(@(x) saveobj(x), obj.Groundtruth);
        end
    end
    
    methods (Static, Access = {?matlab.unittest.TestCase, ?ros.Message})
        function obj = loadobj(strObj)
            %loadobj Implements loading of message from MAT file
            
            % Return an empty object array if the structure element is not defined
            if isempty(strObj)
                obj = ros.custom.msggen.radar_msgs.RadarProcessedArray.empty(0,1);
                return
            end
            
            % Create an empty message object
            obj = ros.custom.msggen.radar_msgs.RadarProcessedArray;
            obj.reload(strObj);
        end
    end
end
